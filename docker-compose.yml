services:
  backend:
    build:
      context: ./backend
    ports:
      - "5001:5001"
    volumes:
      - ./backend/storage:/app/storage
    restart: always

  frontend:
    build:
      context: ./frontend
    ports:
      - "80:80"
    restart: always
